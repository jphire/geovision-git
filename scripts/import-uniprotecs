
#!/bin/bash
source `dirname $0`/inc/common.bash

if [ "$1" = "" ]
then
	echo "usage: $0 /path/to/uniprot_ecs/uniprot_sprot.ecs"
fi
if [ ! -r "$1" ]
then
	errorecho "Could not open the ecs file: $1"
fi

rundjango text_to_db/uniprot_ecs_parser.py $1

